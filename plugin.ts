import type { IApi } from "@umijs/max";
// var OpenBrowserPlugin = require('webpack-open-browser-plugin');
export default (api: IApi) => {
  // api.chainWebpack((memo:any) => {
  //   memo
  //     .plugin("webpack-open-browser-plugin")
  //     .use(OpenBrowserPlugin, [{ url: "http://localhost:3000" }]);
  // });
};
